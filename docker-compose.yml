services:
  web:
    container_name: foodflow-frontend
    build: .
    ports:
      - "4174:4174"
      - "80:80"
    volumes:
     - .:/app
     - ./node_modules:/app/node_modules
    env_file:
      - .env